<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:top="-5dp" android:right="-5dp" android:left="-5dp">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" /> <!-- Imposta uno sfondo trasparente -->
            <stroke
                android:width="2dp"
                android:color="@color/black"/> <!-- Larghezza del bordo -->
            <!-- Colore del bordo -->
        </shape>
    </item>
</layer-list>